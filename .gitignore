node_modules/
dist/
.turbo/
coverage/
*.tsbuildinfo
.env
.env.local
*.log
plan.json

# Terraform
.terraform/
*.tfstate
*.tfstate.backup
crash.log
.workspace/

# Helm
charts/*.tgz
deploy/helm/**/charts/

# orun per-job staged workspaces
.orun/runs/
.orun/objectmodel/

# Generated by `orun compositions lock` / `orun plan`. Committed only once the
# catalog is published and the lock resolves the real OCI digest — a lock
# generated against a locally-packed archive records a machine-local path and
# would point CI at a file that does not exist there.
.orun/compositions.lock.yaml
.orun/component-tree.yaml
